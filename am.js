'use strict'
var ele = document.getElementById("we")   
var pos
var center = window.width/2
var posi
console.log(center)
console.log(window.width)

while (pos >= center) {
    pos+10
    posi = pos + px 
    ele.style.left = posi
    await(10)
}