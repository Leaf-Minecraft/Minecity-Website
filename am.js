'use strict'
var ele = document.getElementById("we")   
var pos
var center = screen.width/2
var posi
console.log(center)
console.log(screen.width)

while (pos >= center) {
    pos+10
    posi = pos + px 
    ele.style.left = posi
    await(10)
}